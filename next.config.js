/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains:["links.papareact.com","https://image.tmdb.org","image.tmdb.org", 
  ],
  }
}

module.exports = nextConfig
